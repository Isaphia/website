source 'https://rubygems.org'

# Local-preview only. GitHub Pages does not read this file —
# it uses its own pinned gem set (the `github-pages` gem). Keep this in
# rough sync with what GitHub Pages currently uses to avoid surprises.

gem 'jekyll', '~> 4.3'
gem 'jekyll-feed', '~> 0.17'

# Ruby 3+ no longer ships webrick by default; Jekyll needs it for `jekyll serve`.
gem 'webrick', '~> 1.8'
