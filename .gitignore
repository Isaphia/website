# Local Jekyll build output and caches
_site/
.jekyll-cache/
.jekyll-metadata

# Local bundler artifacts (Gemfile.lock is also excluded — github-pages pins its own gem set,
# and Actions regenerates it from the Gemfile on each build)
vendor/
.bundle/
Gemfile.lock

# macOS / Dropbox noise
.DS_Store
._*
.dropbox*

# Internal marketing collateral — never publish to the live site
"ASM Isaphia Marketing/"

# Local-only docs and archive assets
CLAUDE.md
logoORG.png
